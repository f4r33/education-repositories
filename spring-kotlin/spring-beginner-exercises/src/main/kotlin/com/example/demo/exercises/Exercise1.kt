package com.example.demo

import org.springframework.web.bind.annotation.RestController

@RestController
class HelloWorldController {

    // TODO: Create a GET endpoint that returns a simple "Hello, World!" message
    // Expected result: When you send a request to "/hello", you should see "Hello, World!" displayed.

}
