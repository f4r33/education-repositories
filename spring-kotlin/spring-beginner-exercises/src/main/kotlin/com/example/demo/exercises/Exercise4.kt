package com.example.demo

import org.springframework.web.bind.annotation.RestController

@RestController
class CounterController {
    private var count = 0

    // TODO: Create a GET endpoint that increments a counter every time it's called and returns the current count.
    // Expected result: Each time you navigate to "/count", the number returned should increment by 1.
}
