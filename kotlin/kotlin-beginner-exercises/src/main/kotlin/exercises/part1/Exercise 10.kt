package exercises.part1


// Exercise 10: Create a Singleton object to manage a counter.
// Goal: Understand how to create and use a singleton object.
// Task: Increment the counter and print its value.

/**
 * Create a singleton called CounterManager with field "counter" and functions "increment" and "getCurrentCount"
 */

fun main() {

    // Increment the counter in counter manager

    // Print the current count in format "Current Count: {currentCount}"

    // Increment the counter in counter manager

    // Print the current count in format "Current Count: {currentCount}"


}
